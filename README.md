# Fundamentals of Computing 1 Project

## Overview
This project contains the C++ code developed for the Fundamentals of Computing 1 course. The specific purpose of the program is not detailed in the repository, but the code demonstrates foundational skills in C++ programming, including the use of standard libraries, build systems (CMake), and core logic implementation. This project represents an early milestone in developing computational thinking and programming abilities.

## Repository Contents
*   **`main.cpp`**: The primary C++ source code file containing the program's logic and functionality.
*   **`CMakeLists.txt`**: The CMake build configuration file used to compile the project.
*   **`cmake-build-debug`**: A directory containing build artifacts and debug information (generated by CMake).

## Technology Used
*   **C++**: The primary programming language for the project.
*   **CMake**: The build system used to manage the compilation process.

## How to Build and Run This Project
1.  Clone this repository to your local machine.
2.  Ensure you have a C++ compiler (like GCC or Clang) and CMake installed.
3.  Create a build directory and navigate into it:
    ```bash
    mkdir build && cd build
